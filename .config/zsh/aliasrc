alias vi=nvim
alias svi="sudo -E nvim"
alias xi="sudo xbps-install"
alias xr="sudo xbps-remove"
alias xq="xbps-query"
alias im="nsxiv"

alias gs='git status'
alias gd='git diff'
alias gl='git log'
alias gp='git push'
alias gpu='git push -u origin main'
alias gpl='git pull'
alias ga='git add'
alias gaa='git add .'
alias gc="git commit -m"
alias gce="git config user.email"
alias gcn="git config user.name"
alias gao='git remote add origin'
alias guo='git remote set-url origin'

alias ls='ls --color=tty'          #size,show type,human readable
alias la='ls -Ah --color=tty'        #long list,show almost all,show type,human readable
alias ll='ls -lAh --color=tty'           #long list
alias lh='ls -ld .* --color=tty'

alias nmount="sudo mount -o gid=non /dev/sdb1 media"
alias unmount="sudo umount /dev/sdb1"

alias cpdoc="cp ~/Ists/Plantilla/plantilla.tex"
alias vidoc="nvim ~/Ists/Plantilla/plantilla.tex"

alias update-grub="sudo grub-mkconfig -o /boot/grub/grub.cfg"

alias tme="tmux a -t me"

alias za="zathura"

alias lg=lazygit

alias bib="nvim ~/Ists/refs.bib"

alias count="find . -type f | wc -l"

alias e="exit"
alias c="clear"

function run() {
    gcc $1 -lm
    ./a.out
}

alias cs="xclip -selection clipboard"
alias vs="xclip -o -selection clipboard"

alias sharescreen="xrandr --output HDMI-1 --mode 1920x1080 --same-as eDP-1"
alias sharescreen_right="xrandr --auto --output HDMI-1 --auto --right-of eDP-1"
alias sharescreen_left="xrandr --auto --output HDMI-1 --auto --left-of eDP-1"
